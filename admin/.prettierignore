dist
node_modules
coverage

# Generated by `supabase gen types typescript` — kept byte-identical to the CLI's raw
# output so CI's type-drift check (a plain diff) doesn't need to run Prettier too.
src/types/database.types.ts
